@* ...existing code... *@

@code {
    // ...existing code...
    [CascadingParameter] public MainLayout? Layout { get; set; }
    // ...existing code...
}

<div class="navmenu @(Layout?.SidebarCollapsed == true ? "collapsed" : string.Empty)">
    <!-- ...existing markup... -->
</div>
